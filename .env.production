################################################################################
# Production Environment Configuration
# Servidor: 147.93.184.62
# Dominio: terap-ia.victalejo.dev
################################################################################

# Database Configuration
DB_HOST=postgres
DB_PORT=5432
DB_USERNAME=postgres
DB_PASSWORD=CHANGE_THIS_SECURE_PASSWORD_IN_PRODUCTION
DB_DATABASE=terapia_db
DB_SSL_ENABLED=false  # Cambiar a true si PostgreSQL tiene SSL configurado

# Backend Configuration
BACKEND_PORT=3100
PORT=3000  # Puerto interno del backend en Docker

# JWT Configuration - ¡CAMBIAR EN PRODUCCIÓN!
JWT_SECRET=CHANGE_THIS_TO_A_VERY_LONG_RANDOM_SECRET_IN_PRODUCTION
JWT_EXPIRATION=7d
JWT_EXPIRES_IN=24h
JWT_REFRESH_EXPIRES_IN=7d

# Google Generative AI (Gemini) - ¡CONFIGURAR CON TU API KEY!
GOOGLE_API_KEY=YOUR_GOOGLE_GEMINI_API_KEY_HERE

# Frontend Configuration
FRONTEND_PORT=3001
# IMPORTANTE: Usar ruta relativa para que el rewrite de Next.js funcione
# El rewrite en next.config.ts redirige /api/v1/* al backend interno de Docker
NEXT_PUBLIC_API_URL=/api/v1

# Production URL (para enlaces absolutos si es necesario)
NEXT_PUBLIC_SITE_URL=https://terap-ia.victalejo.dev

# Node Environment
NODE_ENV=production

# Backup Configuration
BACKUP_DIR=/var/backups/terap-ia
RETENTION_DAYS=30

# WhatsApp Notifications (para scripts de despliegue)
WHATSAPP_API_URL=https://wapi.iaportafolio.com/api/sendText
WHATSAPP_API_KEY=ZR1UZEUaANUd2UUke3ZTbdFtCrXEwQV7
WHATSAPP_CHAT_ID=120363403770897835@g.us
WHATSAPP_SESSION=victalejo

# Server Configuration
DOMAIN=terap-ia.victalejo.dev

################################################################################
# IMPORTANTE: ANTES DE DESPLEGAR A PRODUCCIÓN
################################################################################
# 1. Cambiar DB_PASSWORD por una contraseña segura
# 2. Cambiar JWT_SECRET por un secreto largo y aleatorio (mínimo 64 caracteres)
# 3. Configurar GOOGLE_API_KEY con tu clave de Google Gemini
# 4. Verificar que WHATSAPP_API_KEY sea correcta
# 5. Si usas SSL en PostgreSQL, cambiar DB_SSL_ENABLED=true
#
# Para generar secretos seguros:
# openssl rand -base64 64
################################################################################
